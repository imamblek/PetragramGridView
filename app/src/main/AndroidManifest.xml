<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.imamblek.petagram">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:label="Petagram">
        <activity android:name="com.imamblek.petagram.ActivityContacto"></activity>
        <activity android:name="com.imamblek.petagram.ActivityAcercade" />
        <activity
            android:name="com.imamblek.petagram.MainActivity"

            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.imamblek.petagram.DetalleMascota"

            android:parentActivityName="com.imamblek.petagram.MainActivity" />
    </application>

</manifest>